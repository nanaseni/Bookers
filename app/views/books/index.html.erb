  <body>
    <h1>Books</h1>
    <table>
     <thead>
      <tr>
        <th>Title</th>
        <th>Body</th>
        <th colspan ="3"></th>
      </tr>
     </thead>
     <tbody>
   <% @index.each do |book| %>
       <tr>
         <td><%= book.title %></td>
         <td><%= book.body %></td>
        <td> <%= link_to "Show", "/books/:id" %> </td>
        <td> <%= link_to "Edit", "/edit" %> </td>
        <td> <%= link_to "Destroy", "/books/:id" %> </td>
       </tr>
      <% end %>
     </tbody>
    </table>

    <h2>New book</h2>
    <%= form_with model: @new do|f| %>

    <div class="field">
      <label for="book_title">Title</label>
    　<text_field><%= f.text_field:title %></text_field>
    </div>

     <div class="field">
      <label for="book_body">Body</label>
       <text_area><%= f.text_area:body %></text_area>
     </div>

    <div class="actions"><%= f.submit 'Create Book' %></div>
  <% end %>
</body>
